/*
    For circular buffer, do your own test
    equivalent to what we do for Dynamic Array
    test each method and make sure it works!


*/

class CircularBuffer {
private:
    int* data;
    int capacity;
    int head;
    int tail;
public:
    CircularBuffer(int capacity) : data(new int[capacity]), capacity(capacity), head(0), tail(0)

};



vector<int>
list<int> 

